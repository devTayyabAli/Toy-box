// DISABLED for iOS Swagger - only frontend-integration.doc.js is loaded (see src/swagger.js)
/**
 * @openapi-disabled
 * /api/v1/version:
 *   get:
 *     tags: [Health]
 *     summary: API version info
 *     description: Returns the active API version and canonical URL prefix (`/api/v1`).
 *     responses:
 *       200:
 *         description: Version metadata
 */

module.exports = {};
